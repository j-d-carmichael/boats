swagger: "2.0"

info:
  description: >-
    This is the swagger definition for a made up application
  version: "1.1.1"
  title: Example
  contact:
    email: john-carmichael@email.it

schemes:
  - "http"
  - "https"

host: localhost

basePath: "/api/v1"

tags:
  - name: weather
    description: Weather data

paths:
  $ref: paths/index.yml

definitions:
  $ref: definitions/index.yml

parameters:
  $ref: parameters/index.yml
