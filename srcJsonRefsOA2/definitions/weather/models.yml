type: object
properties:
  cloudCover:
    type: string
  humidity:
    type: number
  precipitation:
    type: number
  temperature:
    $ref: '#/definitions/Units'
  windSpeed:
    $ref: '#/definitions/Units'
  windDirection:
    $ref: '#/definitions/Units'
